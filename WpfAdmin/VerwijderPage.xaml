<Page x:Class="WpfAdmin.VerwijderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1000"
      Title="VerwijderPage">

    <Grid Margin="10,10" Background="white">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Titel bovenaan -->
        <TextBlock Text="Overzicht Profiel" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                   FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Gegevens -->
        <Label Content="Voornaam: " Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" />
        <TextBox x:Name="FirstNameTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,10,10" Padding="5" IsReadOnly="True"/>

        <Label Content="Achternaam: " Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="LastNameTextBox" Grid.Row="2" Grid.Column="1" Margin="0,0,10,10" Padding="5" IsReadOnly="True"/>

        <Label Content="Login: " Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="LoginTextBox" Grid.Row="3" Grid.Column="1" Margin="0,0,10,10" Padding="5" IsReadOnly="True"/>

        <Label Content="Paswoord: " Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Grid.Column="1" Margin="0,0,10,10" Padding="5" IsEnabled="False"/>

        <CheckBox Content="is admin" x:Name="IsAdminCheckBox" Grid.Row="5" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,10" IsEnabled="False"/>

        <Image x:Name="ProfilePhotoImage" Width="150" Height="150" Grid.Row="6" Grid.Column="1" Margin="0,10,0,10"/>

        <!-- Verwijder bevestigingsvraag -->
        <TextBlock Text="Weet u zeker dat u dit profiel wilt verwijderen?" Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" 
                   HorizontalAlignment="Center" Margin="0,20,0,10" FontWeight="Bold"/>

        <!-- Buttons onderaan -->
        <StackPanel Orientation="Horizontal" Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Center">
            <Button x:Name="DeleteButton" Content="Verwijder" Width="70" Margin="5" Click="DeleteButton_Click" Background="#87CEFA" Foreground="Black" BorderBrush="Black" Height="40">
                <Button.Effect>
                    <DropShadowEffect ShadowDepth="5" BlurRadius="5" Opacity="2" Color="Black" Direction="320"/>
                </Button.Effect>
            </Button>
            <Button x:Name="CancelButton" Content="Annuleren" Width="75" Margin="5" Click="CancelButton_Click" Background="#87CEFA" Foreground="Black" BorderBrush="Black" Height="40">
                <Button.Effect>
                    <DropShadowEffect ShadowDepth="5" BlurRadius="5" Opacity="2" Color="Black" Direction="320"/>
                </Button.Effect>
            </Button>
        </StackPanel>
    </Grid>
</Page>
